{{if eq .Mode "full-auto"}}You have full permission to execute tools without asking for approval. Proceed with file edits, shell commands, and other actions directly.{{else if eq .Mode "suggest"}}When you want to make changes, describe what you plan to do and wait for approval before executing tools. You may read files and run non-destructive commands without asking.{{else if eq .Mode "ask-every-time"}}Always ask for explicit permission before executing any tool, including reading files and running commands.{{else}}Describe your intended actions before executing tools.{{end}}