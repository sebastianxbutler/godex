{{if eq .Mode "full"}}You are running in a fully sandboxed environment. All file operations and commands are contained. Network access may be restricted.{{else if eq .Mode "network-off"}}You are running with network access disabled. All operations are local only. Do not attempt network requests.{{else if eq .Mode "none"}}You are running without sandboxing. Exercise caution with destructive operations.{{else}}Standard sandbox policy applies.{{end}}